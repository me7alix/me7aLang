import "std/libc.m7"

macro arr_len(arr) {
	(sizeof(arr) / sizeof(*(arr))) \
}

macro new(T) {
	(calloc(1, sizeof:T): *T) \
}

macro swap(a, b) {
	block {
		tmp := a
		a = b
		b = tmp
	}
}

macro do_while(expr, body) {
    while true {
        block body
        if !(expr) do break
    }
}
