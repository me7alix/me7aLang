extern print_int(var: int)
extern print_char(var: i8)
extern input_int(): int

func main(): int {
	print_char('c')
	print_char('n')
	print_char('t')
	print_char(':')
	print_char(' ')

	cnt := input_int()
	a: int = 1
	b: int = 2

	for i := 0; i < cnt; i=i+1 {
		print_int(a)

		l := b
		b = b + a
		a = l
	}

	return 0
}
