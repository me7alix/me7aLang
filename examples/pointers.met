extern malloc(cnt: i64): *u0
extern free(ptr: *u0)
extern print_int(val: int)

func main(): int {
	nums := malloc(40:i64):*int

	for i: i64 = 0; i < 10; i=i+1 {
		*(nums + i) = (i * i):int
	}

	for i: i64 = 0; i < 10; i=i+1 {
		print_int(*(nums + i))
	}
	
	free(nums)
	return 0
}
