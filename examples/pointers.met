extern malloc(cnt: iptr): *u0
extern free(ptr: *u0)
extern print_int(val: int)

func main(): int {
	nums := malloc(sizeof:int * 10):*int

	for i: iptr = 0; i < 10; i=i+1 {
		*(nums + i) = (i * i):int
	}

	for i: iptr = 0; i < 10; i=i+1 {
		print_int(*(nums + i))
	}
	
	free(nums)
	return 0
}
