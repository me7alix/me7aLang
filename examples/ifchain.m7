import "std/libc.m7"

fn main() {
	for i := 0; i < 20; i+=1 {
		if      i < 5           do printf("a")
		else if i < 13 && i > 7 do printf("b")
		else if i == 15         do printf("X")
		else                    do printf(" ")
		printf(" %d\n", i)
	}
}
