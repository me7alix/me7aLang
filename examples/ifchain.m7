import "std/libc.m7"

fn main() {
	for i := 0; i < 20; i+=1 {
		if      i < 5           then printf("a")
		else if i < 13 && i > 7 then printf("b")
		else if i == 15         then printf("X")
		else                    then printf(" ")
		printf(" %d\n", i)
	}
}
